// Test moved to src/features/portfolio/components/Hero.test.tsx
import { describe } from 'vitest'

describe.skip('Hero (moved to feature)', () => {
  // test intentionally skipped to avoid duplicate coverage
})
